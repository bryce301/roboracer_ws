# RoboRacer ROS 2 Workspace

Personal source snapshot of `/home/bryce/roboracer_ws`.

The ROS 2 packages are under `src/`. Generated workspace directories such as
`build/`, `install/`, and `log/` are intentionally excluded and should be
recreated locally with `colcon build`.
